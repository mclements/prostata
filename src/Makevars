PKG_LIBS = `$(R_HOME)/bin/Rscript -e "microsimulation:::LdFlags()"` $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

PKG_CXXFLAGS = -I. -DBOOST_NO_AUTO_PTR
PKG_CFLAGS = -I.

OBJECTS = prostata.o prostata-init.o utils.o
